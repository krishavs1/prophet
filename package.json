{
  "name": "defai-sentinel",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "workspaces": [
    "frontend",
    "contracts",
    "agent"
  ],
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=18"
  }
}
