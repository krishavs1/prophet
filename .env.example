# 0G Integration
# Get your API key from https://0g.ai or your 0G provider
ZERO_G_API_KEY=your_0g_key_here

# 0G network (agent uses this for inference)
# Mainnet (real 0G token):
# 0G_RPC_URL=https://evmrpc.0g.ai
# Testnet (faucet tokens): omit 0G_RPC_URL or set:
# 0G_RPC_URL=https://evmrpc-testnet.0g.ai
# Legacy / other: RPC_URL_SEPOLIA is fallback if 0G_RPC_URL not set
RPC_URL_SEPOLIA=https://evmrpc-testnet.0g.ai

# Deployment
# Private key for testnet deployments (NEVER commit this to git)
# Format: 0x... (64 hex characters, no 0x prefix in value)
PRIVATE_KEY_DEPLOYER=0x...

# Optional: Mainnet RPC (read-only analysis)
# RPC_URL_MAINNET=https://mainnet.infura.io/v3/YOUR_INFURA_KEY

# 0G Storage (decentralized audit persistence)
# Indexer endpoint for 0G storage network
ZG_INDEXER_RPC=https://indexer-storage-testnet-turbo.0g.ai

# Foundry Sandbox
# Docker sandbox is ON by default. Set to false for local dev without Docker.
# USE_DOCKER_SANDBOX=false
# FORGE_DOCKER_IMAGE=prophet-forge
# FORGE_TIMEOUT_MS=120000
