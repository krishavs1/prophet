{
  "name": "@prophet/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Contracts dev - to be implemented'",
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "format": "forge fmt",
    "check-foundry": "forge --version || (echo 'Foundry not found. Install with: curl -L https://foundry.paradigm.xyz | bash && foundryup' && exit 1)"
  }
}
